# Phase I software — private lab forge

This repository is the **private** GitHub stand-in for unfinished Codex Solbian Phase I software while Helios / Gitea are down.

- Public product hub remains `pegamonstro/solbian-codex` (organisation docs / intentional archives only).
- Living Journey SQLite stores stay Mac-local (`**/*.sqlite` ignored).
- Secrets / `.env*` stay Mac-only — never commit them here.
- When Helios returns, this may be re-synced to the private Gitea lab forge; do not treat this as a second public product.
